File: [`frontend/src/types/types.ts`](../../../../frontend/src/types/types.ts)

**What It Does**
- TypeScript types for API responses and UI models.
- Includes GraphQL query data and variables interfaces.

**Why This Structure**
- Keeps type definitions centralized and reusable.
- Reduces duplication across pages and components.

**Principles**
- Type safety.
- Shared contracts across the UI.


**Principles Explained**
- `Type safety`: Use TypeScript types to catch errors early and keep contracts explicit.
- `Shared contracts across the UI`: Types/interfaces are shared so data shapes stay aligned.
